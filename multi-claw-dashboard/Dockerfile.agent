FROM python:3.11-slim
WORKDIR /app
COPY multi-claw-agent/pyproject.toml .
COPY multi-claw-agent/src/ ./src/
RUN pip install --no-cache-dir -e .
EXPOSE 8100
ENTRYPOINT ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8100"]
